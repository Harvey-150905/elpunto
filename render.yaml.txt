services:
  - type: web
    name: elpunto-backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn elpunto.wsgi
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: elpunto.settings
      - key: PYTHON_VERSION
        value: 3.12.7
      - key: DEBUG
        value: False
      - key: RENDER
        value: True
    staticPublishPath: staticfiles

    # 👇 Esto hace que Render sirva también los archivos de media
    routes:
      - type: rewrite
        source: /media/(.*)
        destination: /media/\1
